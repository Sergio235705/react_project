import React from 'react';

export const AuthContext= React.createContext({authUser:{}});
export const LanguageItalianContext=React.createContext(null);
export const ThemeDarkContext=React.createContext(null)
export const BrandsContext = React.createContext();
export const RentContext=React.createContext();
